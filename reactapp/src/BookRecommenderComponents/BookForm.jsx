import React from "react";

function BookForm() {

  const validateForm = () => {
    return true
  }

  const handleSubmit = (e) => {

    e.preventDefault()
    if(validateForm()){
      
    }

  };

  return <div></div>;
}

export default BookForm;
